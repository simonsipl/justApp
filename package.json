{
  "name": "employee-worker",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "dependencies": {
    "@hapi/joi": "^16.1.4",
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "lodash.mapvalues": "^4.6.0",
    "mongodb": "^3.3.2"
  },
  "devDependencies": {
    "airbnb": "0.0.2",
    "cross-env": "^6.0.0",
    "eslint": "^6.4.0",
    "eslint-plugin-node": "^10.0.0",
    "husky": "^3.0.5",
    "lint-staged": "^9.4.0",
    "nodemon": "^1.19.2",
    "prettier-eslint-cli": "^5.0.0"
  },
  "lint-staged": {
    "**/*.js": [
      "eslint --fix",
      "prettier-eslint --write",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "scripts": {
    "start": "cross-env NODE_ENV=production node src/server.js",
    "dev": "cross-env NODE_ENV=dev nodemon src/server.js",
    "lint": "cross-env eslint src/**/*.js --fix",
    "format": "cross-env prettier-eslint 'server/**/*.{js,json}' --write"
  },
  "author": "",
  "license": "ISC"
}
